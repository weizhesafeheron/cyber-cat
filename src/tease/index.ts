/**
 * 逗猫棒。
 *
 * 光标本身就是逗猫棒，没有开关也没有按钮 - 逗猫变成用户本来就在做的事。
 * 但它是**邀请而非命令**：必须通过六道闸门，任何一道不满足就完全无视光标
 * （issue #11）。六道闸门是纯函数，见 gates.ts。
 *
 * 这一层不碰 DOM、不读时钟、不持有平台能力。时刻由调用方注入，
 * 键盘活跃度由平台层探测后传进来。
 */
export * from './constants.js';
export * from './gates.js';
export * from './pounce.js';
