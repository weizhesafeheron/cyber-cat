# GitHub 帮你构建两个平台的安装包。
#
# 为什么必须在各自的平台上构建：Windows 的包要 MSVC 工具链与资源编译器，
# macOS 的包要 Xcode 的签名与 hdiutil，两边都不能交叉编译。所以是一个矩阵，
# 而不是一台机器出两份产物。
#
# 什么时候跑：
# - 手动点（workflow_dispatch）：**默认两个平台都出**，产物存成 artifact，
#   在那次运行的页面里下载（zip），保留 30 天。
#   platform 那个下拉能只挑一个平台跑 - 只在「明确只要其中一个」时用它省 runner 时间
#   （private 仓库里 macOS 按 10 倍计费），但默认必须是两个都有，
#   因为这条路的意义就是「不装任何开发环境也能拿到两个平台的包」。
# - 推 v 开头的 tag：额外建一个**草稿** Release 并把安装包挂上去。草稿是刻意的 -
#   发布前你还要自己在 macOS 与 Windows 上各装一次（见 issue #16）。
# - **不在每次 push 到 main 时跑。** macOS runner 按 10 倍计费，而这个仓库的提交
#   绝大多数只改前端与文档，没必要每次都烧两台机器。测试那个 job 才是每次都跑的。
name: build

on:
  workflow_dispatch:
    inputs:
      platform:
        description: 构建哪个平台
        type: choice
        default: both
        options:
          - windows
          - macos
          - both
  push:
    tags:
      - 'v*'
    branches:
      - main
  pull_request:
    branches:
      - main

# 同一个分支上新的一次推送会取消上一次还没跑完的，省掉排队的 runner
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # 每次 push 与 PR 都跑。跑在 ubuntu 上：测试全是 node 里的纯逻辑，
  # 不需要 webview 也不需要 Rust - 世界层与渲染层本来就是平台无关的。
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run typecheck
      - run: npm test

  build:
    # tag 与手动触发才构建。push 到 main 只跑上面那个 test。
    if: github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/v')
    permissions:
      # 建 Release 要写权限。只有这个 job 需要。
      contents: write
    strategy:
      # 一个平台挂了另一个照样出包 - 只有一半能装也比两手空空有用
      fail-fast: false
      matrix:
        # 手动触发时按 platform 输入过滤；tag 触发一律两个都出（要发版就得两个包齐）。
        # exclude 里的条件为真时那一行被剔掉。
        exclude:
          - platform: ${{ (github.event_name == 'workflow_dispatch' && github.event.inputs.platform == 'windows') && 'macos-latest' || '' }}
          - platform: ${{ (github.event_name == 'workflow_dispatch' && github.event.inputs.platform == 'macos') && 'windows-latest' || '' }}
        include:
          - platform: macos-latest
            # universal：一个 DMG 同时支持 Apple Silicon 与 Intel。
            # 代价是 Rust 要编两遍（macOS runner 上大约多五分钟），
            # 换来的是下载的人不需要先搞清楚自己的芯片是哪种。
            args: --target universal-apple-darwin
            rust-targets: aarch64-apple-darwin,x86_64-apple-darwin
          - platform: windows-latest
            args: ''
            rust-targets: ''
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-targets }}

      # Rust 的编译产物缓存。第一次跑要十几分钟，之后只重编我们自己的 crate。
      # workspaces 指到 src-tauri：Cargo.toml 在那儿，不在仓库根上。
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          key: ${{ matrix.platform }}

      - run: npm ci

      # tauri-action 会调 `tauri build`，而它自己会先跑 beforeBuildCommand
      # （npm run build = tsc --noEmit && vite build），所以前端不用单独构建一步。
      - uses: tauri-apps/tauri-action@v0
        id: tauri
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # 签名与公证：仓库里现在没有这些 secret，所以产出的是 ad-hoc 签名的包。
          # 配上之后这一步会自动签名并公证，不用改 workflow。
          # macOS：APPLE_CERTIFICATE（base64 的 .p12）、APPLE_CERTIFICATE_PASSWORD、
          #        APPLE_SIGNING_IDENTITY、APPLE_ID、APPLE_PASSWORD、APPLE_TEAM_ID
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        with:
          args: ${{ matrix.args }}
          # tagName 为空时 tauri-action 只构建、不碰 Release。
          # 推 tag 才建，而且是草稿：发布前还要在两个平台各装一次。
          tagName: ${{ startsWith(github.ref, 'refs/tags/v') && github.ref_name || '' }}
          releaseName: ${{ startsWith(github.ref, 'refs/tags/v') && github.ref_name || '' }}
          releaseDraft: true
          releaseBody: |
            桌面上一只 24 小时都在生活的像素猫。功能与已知限制见 README。

            **两个包都没有签名。**
            - macOS：第一次打开要右键点 → 打开 → 再点一次「打开」，或者去「系统设置 → 隐私与安全性」点「仍要打开」。
            - Windows：SmartScreen 会拦一下，点「更多信息」→「仍要运行」。需要 WebView2 运行时，Windows 10 上安装包会自己下载。

      # 手动触发时把产物存成 artifact：那种情况下没有 Release 可挂。
      # if-no-files-found: error - 静默地上传一个空压缩包比失败更难查。
      - uses: actions/upload-artifact@v4
        if: ${{ !startsWith(github.ref, 'refs/tags/v') }}
        with:
          name: cybercat-${{ matrix.platform }}
          if-no-files-found: error
          retention-days: 30
          path: |
            src-tauri/target/*/release/bundle/dmg/*.dmg
            src-tauri/target/release/bundle/msi/*.msi
            src-tauri/target/release/bundle/nsis/*.exe
